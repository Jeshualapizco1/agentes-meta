{
  "name": "agentes-meta",
  "private": true,
  "packageManager": "pnpm@11.25.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "collector": "pnpm --filter @agentes-meta/agents collector",
    "db:migrate": "pnpm --filter @agentes-meta/db migrate"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "tsx": "^4.20.0",
    "vitest": "^3.2.4",
    "@types/node": "^24.3.0"
  }
}
